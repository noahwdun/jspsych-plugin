/**
 * Slightly more robust version of resolve
 *
 * Prevents issue where only / is returned if passed in one segment
 * @returns {string}
 */
export function resolve(...pathSegments: any[]): string;
export declare let join: (...paths: string[]) => string;
